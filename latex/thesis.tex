\documentclass{thesis}

\usepackage[inkscapeformat=png]{svg}
\usepackage{siunitx}
\usepackage{tikz}
\usepackage{circuitikz}
\usepackage{pgfplots}
\usetikzlibrary {positioning,fit,calc,shapes.geometric}

\newcommand{\todo}[1]{{\color{red}TODO: #1}}

% I don't know whether to talk in first person plural or singular
\newcommand{\We}{We}
\newcommand{\we}{we}
\newcommand{\our}{our}
\newcommand{\Our}{Our}

% Units
\newcommand{\V}{\unit{\volt}}
\newcommand{\mV}{\unit{\milli\volt}}
\newcommand{\uV}{\unit{\micro\volt}}
\newcommand{\nV}{\unit{\nano\volt}}
\newcommand{\Hz}{\unit{\hertz}}
\newcommand{\kHz}{\unit{\kilo\hertz}}
\newcommand{\ms}{\unit{\milli\second}}
\newcommand{\us}{\unit{\micro\second}}
\newcommand{\cm}{\unit{\centi\meter}}
\newcommand{\mm}{\unit{\milli\meter}}
\newcommand{\K}{\unit{K}}
\newcommand{\ohm}{\unit{\ohm}}
\newcommand{\kohm}{\unit{\kilo\ohm}}
\newcommand{\dB}{\unit{\deci\bel}}
\newcommand{\dBV}{\unit{\deci\bel\volt}}
\newcommand{\uF}{\unit{\micro\farad}}

% Signal names
\newcommand{\drdy}{{\ensuremath{\mathtt{DOUT/}\overline{\mathtt{RDY}}}}}

\addbibresource{bibliography.bib}

\begin{document}

\defaultfont

\maketableofcontents

\section{Introduction (2-3pg)}

In electrophysiology, two prevalent modes of biopotential measurements are electrocardiography (ECG) and electromyography (EMG), measuring cardiac muscle potentials and skeletal muscle potentials respectively. While methods that use intracellular electrodes exist and are much more precise, using surface electrodes is very common thanks to their non invasive nature, albeit at a loss of spatial specificity due to them being limited to measuring a superposition of nearby potentials. The field of ECG is by and large applied to identifying and diagnosing cardiac abnormalities in clinical and first responder settings, focusing on disturbances of the cardiac rhythm, artery blood flow, or electrolyte availability for diagnosis of heart conditions. On the other hand, Surface EMG (henceforth sEMG) has broader applications, ranging from the clinical, such as diagnosing nerve and muscle damage, to prosthetics, rehabilitation, biofeedback therapy, ergonomics, sports and movement science \cite{Barbero2012}, and it also opens up the possibility of novel Human-Computer Interaction methods.

Robot Operating System (ROS) is an open-source software framework for robotic applications, most importantly providing a flexible communication system ("the plumbing") that allows for real-time message-passing between networked nodes.\cite{ros} In practice, these nodes can be any component of the larger robotic system, be them anything from a discrete piece of hardware to a software component. Nodes communicate by sending messages, which can have varying levels of complexity, from primitives to nested aggregated structures, via "topics" which implement publish/subscribe semantics. In our application, the biopotential DAQ node can expose topics on which it publishes measured samples, for any other node to subscribe to for further signal processing.

The Linux Industrial I/O (IIO) subsystem was created in 2008 and mainlined in 2012 (\href{https://github.com/torvalds/linux/commit/06458e277eac2b8761b0a04d3c808d57be281a2e}{commit \texttt{06458e2}}) to provide a simple and unified way to interact with devices with ADC or DAC capabilities, broadly ranging from plain ADC/DAC chips to Inertial Measurement Units (Accelerometers, Gyroscopes, Magnetometers), Software Defined Radio, robotic actuators and sensors, and more. Within the Linux kernel, it fills in a gap between the \textit{input} subsystem which can stream user input data and the \textit{hwmon} subsystem used for monitoring and controlling hardware state such as CPU temperature and fan settings, albeit with low refresh rate. The IIO subsystem provides a modular way to define high bandwidth communication with input/output devices directly connected to the host via SPI or I2C and specified as part of the device tree. Despite the subsystem's name, it is not limited to industrial production applications: IIO usage is ubiquitous in today's linux-based devices for integrating features such as accelerometers, gyroscopes or ambient light sensors in a uniform way, besides other hardware abstraction layers.

\textit{Libiio} is a library which provices cross-platform functionality for interacting with both local and remote IIO devices within the same framework. To accomplish this, it implements the IIO Daemon (IIOD) for which it defines the \textit{IIOD protocol}. The current version of the IIOD protocol (v0.1) is based on a set of human readable commands sent via a single data stream of alternating client requests and server responses. This allows the same protocol to be used over a variety of transport layers, officially Serial (most relevant for this application), USB and TCP.

Although the electrical design of biopotential measurement devices has been studied for hundreds of years, tracing back to Luigi Galvani's work in the 18th century, and there exist a number of current-day commercial, academic, and DIY solutions, only a small part aspire to the ideals of Open Source Hardware and/or Open Source Software (ASPEN\cite{Esposito2018}, openBCI\cite{openbci}, BioAmp-EXG-Pill\cite{pill}, MyoWare EMG\cite{myoware}), and there exist no systems that integrate with ROS2 or the Linux IIO subsystem, limiting their flexibility in terms of building software that seamlessly interfaces with them.

We aim to fill this gap and provide an affordable and easily maintainable hardware and software solution for measuring multi-channel biopotential signals and interfacing them with ROS2 and LibIIO. Adapting this system to work with other software suites should be made as simple as possible thanks to the versatility of both ROS2 and LibIIO, and adapting it to other hardware platforms should also be facilitated by the decoupled nature of the hardware, firmware and software parts. An overview of the information flow, from the biological signals to the software, in figure \ref{fig:flow}.

\begin{thesisfigure}{Overview of information flow}{flow}
\begin{tikzpicture}[
    node distance=10mm,
    component/.style={
        rectangle, thick, draw=black
    },
    system/.style={
        rectangle, thick, dashed, draw=black!40, text=black!40
    }
]
    \node (pac) [] { Pacemaker cells };
    \node (car) [component, right=of pac] { Cardiac muscle contraction };
    \node (paccar) [fit={(pac) (car) ([yshift=1.5em]pac.north)},system,label={[text=black!40,anchor=north]north:ECG}] {};

    \node (ner) [below=12mm of pac.south west, anchor=north west] { Nerve signal };
    \node (ske) [component, below=12mm of car.south west, anchor=north west] { Skeletal muscle contraction };
    \node [fit={(ner) (ske) ([yshift=1.5em]ner.north)},system,label={[text=black!40,anchor=north]north:EMG}] {};

    \node (midcarske) at ($(car)!1!(ske)$) {};

    \node (pot) [component, right=35mm of midcarske] { Potential fluctuations };

    \draw [->, thick] (pac) edge (car);
    \draw [->, thick] (car.east) edge (pot.north west);

    \draw [->, thick] (ner) edge (ske);
    \draw [->, thick] (ske.east) edge (pot.west);

    \node (ele) [component, below=15mm of pot] { Surface electrodes };
    \node (adc) [component, left=of ele] { ADC };
    \node (amp) [component, below=of $(ele)!0.5!(adc)$] { Amplifier };

    \node [fit={(ele) (amp) (adc) ([yshift=1.5em]ele.north)},system,label={[text=black!40,anchor=north]north:Electronics}] {};

    \path [->, draw, thick]
        (pot) edge (ele)
        (ele) edge (amp)
        (ele) edge (adc)
        (amp) edge (adc);

    \node (dri) [component, left=23mm of adc] { ADC driver };
    \node (dev) [component, left=of dri] { IIO device };
    \node (iod) [component, below=of dev] { IIO daemon };
    \node (buf) [component, right=of iod] { Sample buffer };

    \node [fit={(dri) (dev) ([xshift=+3mm]buf.south east) ([xshift=-3mm]iod.west) ([yshift=1.5em]dev.north)},system,label={[text=black!40,anchor=north]north:Firmware running on microcontroller}] {};

    \path [<->, draw, thick]
        (adc) edge node[very near start,above,anchor=south east] {SPI} (dri)
        (dri) edge (dev)
        (iod) edge (dev);

    \path [->, draw, thick]
        (dev) edge (buf)
        (buf) edge (iod);

    \node (lib) [component, below=18mm of iod] { LibIIO };
    \node (pyt) [component, right=5mm of lib] { Python bindings };
    \node (rem) [below=15mm of lib.west,anchor=west] { Remote LibIIO instance };

    \path [<->, draw, thick]
        (iod) edge node [very near start, right, anchor=north west] {Serial over USB} (lib)
        (lib) edge (pyt.west)
        (lib) edge (rem);

    \node (ros) [component, right=5mm of pyt] { ROS2 publisher node };
    \node (net) [right=of ros] { ROS2 network };

    \path [<->, draw, thick]
        (pyt) edge (ros)
        (ros) edge (net);


    \node [fit={(ros) (pyt) ([yshift=1.5em]lib.north west)},system,label={[text=black!40,anchor=north]north:Software running on host}] {};
\end{tikzpicture}
\end{thesisfigure}

\newpage
\section{Bibliographic research (15pg)}

% (current state of the art + problem justification + possible approaches)

\subsection{Anatomical basis}

% (car), (ske), (pot)

As part of normal function, all biological cells have a difference of electrical potential between their interior and their exterior, called the \textit{membrane potential}. In the steady state, the \textit{resting membrane potential} of both skeletal muscle cells and that of cardiomyocytes (cardiac muscle cells) is in the $\numrange{-70}{-90} \mV$ range \cite{Hopkins2006,Klabunde2011-jo}.

\subsubsection*{Skeletal muscles: sEMG}

From an electrophysiological standpoint, skeletal muscles are comprised of a number of motor units (MU), each corresponding to an individual axon that innervates a number of muscle fibers. Neural impulses travel from the central nervous system via nerves to a motor unit's innervation zone (IZ), where a number of neuromuscular junction synapses trigger an action potential in the fibers of that motor unit. The electrical activation of the muscle doesn't take place all across the fiber simulataneously, but rather travels from the innervation zone towards the tendons as an impulse.

This starts out as an action potential (AP) at the innervation zone, wherein the span of 4-6ms, the membrane potential rises from the resting $-70 \mV$ to positive (depolarisation) and then back to the resting potential (repolarisation) by biomolecular mechanisms involving the flow of $\text{Na}^+$, $\text{K}^+$ and $\text{Cl}^-$ ions. This localized action potential also triggers action potential in neighbouring parts of the muscle cell, leading to a chain reaction down the muscle fiber with a clear external electrical manifestation: the Motor Unit Action Potential as a traveling impulse of ion flow, which is measurable via the potential fluctuations across tissue.

Muscle fiber contractions follow the "all or nothing" principle, meaning a fiber either contracts fully or not at all. To modulate the force excerted by a muscle, only a part of the available motor units are recruited. Still, there is an independence between the number of motor units, number of fibers, and a muscle's strength, the motor unit count only improving the "resolution" of muscle control. By Henneman's size principle \cite{Henneman1957}, low effort contractions start by recruiting smaller motor units and gradually activate larger motor units as load increases. Besides this spatial control, muscular contraction is also controlled via rate coding, where the same motor unit is reactivated repeatedly, with the activation frequency positively correlating with the overall contraction strength, until fatigue is reached \cite{DeLuca1985}.

As a practical result, during low effort motion where only one or a handful of motor units are engaged, the APs are very easily identifiable in the measured waveform. On the other hand, high effort contractions will manifest a superposition of uncorrelated MUAPs which are difficult to separate from just sEMG measurements. (figure \ref{fig:apmuap})

\begin{thesisfigure}{Individually identifiable action potentials during a low-effort period (left). Superposition of action potential during a higher-effort movement (right). Electrodes placed $35\mm$ apart on inner forearm.}{apmuap}
\includegraphics[width=0.45\linewidth, trim=5cm 10cm 3cm 10cm]{emg ap.pdf}
\includegraphics[width=0.45\linewidth, trim=5cm 10cm 3cm 10cm]{emg muap.pdf}\\
\vspace{0.5cm}
\end{thesisfigure}

Surface measurements have worse spatial resolution and cannot precisely pinpoint the source of the observed electrical activation, but rather "see" a potential halo. In the case of muscle fibers, the surface halo of a travelling action potential is modelled as a directional tripole with a leading positive depolarizing area, a trailing positive repolarizing area, and a strongly negative spike in the middle.\cite[Section 3]{Barbero2012}

\subsubsection*{Cardiac muscles: ECG}

While the internal mechanisms of muscle contractions are similar between cardiac and skeletal muscle, their external activation strategies vary almost entirely. Cardiac muscle isn't divided into motor units and as such heartbeat strength cannot be modulated by recruitment, but rather by various chemical and mechanical factors that ultimately increase or decrease the availability of $\text{Ca}^{2+}$ ions, such as but not limited to: the Frank-Starling mechanism (which aids in stroke volume regulation), or hormone effects from secretion of adrenaline or norepinephrine.

Contrasted to the usual sEMG measurement, all healthy heartbeats have a clearly identifiable structure consisting of a P wave, the QRS complex, and a T wave. \todo{FIGURE} Cardiac conditions can be characterized and diagnosed by these features' timing, but their electrical characteristics are very well defined, especially when compared to the noisy-looking MUAP superposition seen during skeletal muscle contraction.

Another main difference between sEMG and ECG is electrode placement. While for sEMG we are interested in getting the electroces as close as possible to the muscle of interest to avoid confusion with other sources of electrical activity, ECG focuses on large scale electric field gradients that appear in the body as a result of a heartbeat. In essence, both the amplitude and "direction" of depolarization and repolarization can be determined by measuring potential differences across a patient's extremities. Localized contractions of cardiac muscle can be identified by the electric field disturbances they create even tens of centimeters away, bypassing the need to have electrodes as close as possible to the heart. All of this is possible in ECG but not EMG because of the very strong electrical impulses created by the heart, as well as their regular, non-superimposed structure. \todo{FIGURE illustrating cardiac dipole model}

\subsection{Electronics for biopotential measurement}

\subsubsection{Biopotential measurement modes}

Two common modes of measurement of biopotentials arise: \textit{unipolar} measurements referred to a common point whose potential isn't affected too much by the observed process and \textit{bipolar} measurements where consecutive electrodes' potentials are taken into differential amplifiers, as illustrated in figure \ref{fig:unibipolar}. Both are relevant for a biopotential data acquisition system, and as such one of our goals is having a system that can do both.

\begin{thesisfigure}{Unipolar (left) and bipolar (right) measurements with 4 electrodes}{unibipolar}
\resizebox{0.6\textwidth}{!}{%
\begin{circuitikz}
\tikzstyle{every node}=[font=\Large]
\draw (6.25,19) to[short, -o] (5,19) ;
\draw [](6.25,16.5) to[short, -o] (5,16.5) ;
\draw (9,18.5) node[op amp,scale=1, yscale=-1 ] (opamp2) {};
\draw (opamp2.+) to[short] (7.5,19);
\draw  (opamp2.-) to[short] (7.5,18);
\draw (10.2,18.5) to[short](10.5,18.5);
\draw (9,16) node[op amp,scale=1, yscale=-1 ] (opamp2) {};
\draw (opamp2.+) to[short] (7.5,16.5);
\draw  (opamp2.-) to[short] (7.5,15.5);
\draw (10.2,16) to[short](10.5,16);
\draw (9,13.5) node[op amp,scale=1, yscale=-1 ] (opamp2) {};
\draw (opamp2.+) to[short] (7.5,14);
\draw  (opamp2.-) to[short] (7.5,13);
\draw (10.2,13.5) to[short](10.5,13.5);
\draw [](6.25,14) to[short, -o] (5,14) ;
\draw [](6.25,11.5) to[short, -o] (5,11.5) ;
\draw [](6.25,11.5) to[short] (7,11.5);
\draw [](7,11.5) to[short] (7,18);
\draw [](7,18) to[short] (7.5,18);
\draw [](7,15.5) to[short] (7.5,15.5);
\draw [](7,13) to[short] (7.5,13);
\node at (7,13) [circ] {};
\node at (7,15.5) [circ] {};
\draw [](6.25,19) to[short] (7.5,19);
\draw [](6.25,16.5) to[short] (7.5,16.5);
\draw [](6.25,14) to[short] (7.5,14);
\node [font=\LARGE] at (5,14.5) {$V_3$};
\node [font=\LARGE] at (5,17) {$V_2$};
\node [font=\LARGE] at (5,19.5) {$V_1$};
\node [font=\LARGE] at (5,12) {$V_{REF}$};

\draw [](15,19) to[short, -o] (13.75,19) ;
\draw [](15,16.5) to[short, -o] (13.75,16.5) ;
\draw (17.75,18.5) node[op amp,scale=1, yscale=-1 ] (opamp2) {};
\draw (opamp2.+) to[short] (16.25,19);
\draw  (opamp2.-) to[short] (16.25,18);
\draw (18.95,18.5) to[short](19.25,18.5);
\draw (17.75,16) node[op amp,scale=1, yscale=-1 ] (opamp2) {};
\draw (opamp2.+) to[short] (16.25,16.5);
\draw  (opamp2.-) to[short] (16.25,15.5);
\draw (18.95,16) to[short](19.25,16);
\draw (17.75,13.5) node[op amp,scale=1, yscale=-1 ] (opamp2) {};
\draw (opamp2.+) to[short] (16.25,14);
\draw  (opamp2.-) to[short] (16.25,13);
\draw (18.95,13.5) to[short](19.25,13.5);
\draw [](15,14) to[short, -o] (13.75,14) ;
\draw [](15.75,18) to[short] (16.25,18);
\draw [](15.75,15.5) to[short] (16.25,15.5);
\draw [](15.75,13) to[short] (16.25,13);
\draw [](15,19) to[short] (16.25,19);
\node [font=\LARGE] at (13.75,14.5) {$V_3$};
\node [font=\LARGE] at (13.75,17) {$V_2$};
\node [font=\LARGE] at (13.75,19.5) {$V_1$};
\draw [](15,16.5) to[short] (16.25,16.5);
\draw [](15.75,18) to[short] (15.75,16.5);
\draw [](15,14) to[short] (16.25,14);
\draw [](15.75,15.5) to[short] (15.75,14);
\draw [](15.75,13) to[short] (15.75,11.5);
\node at (15.75,14) [circ] {};
\node at (15.75,16.5) [circ] {};
\draw [](15,11.5) to[short, -o] (13.75,11.5) ;
\draw [](15,11.5) to[short] (15.75,11.5);
\node [font=\LARGE] at (13.75,12) {$V_4$};
\end{circuitikz}
}
\end{thesisfigure}

\subsubsection{Driven Right Leg}

In both measurement modes, having the patient's body properly referred to a known electrical node is very important for the proper functioning of amplification and sensing circuitry. If the patient is "disconnected" from the rest of the circuit, and only interfaced to with high impedance sensing electrodes, stray charges originating from various sources can push the entire patient's body to common-mode potentials above or below the measurable range. Static electricity from clothing can regularly build up hundreds or thousands of volts, which easily exceeds the measurement range of common ADCs or amplifiers. While electrostatic discharge can be accounted for with protection circuitry, such high common-mode voltages make signals unrecoverable.

The solution is having a current path from the observed patient to the measurement aparatus' ground, through which charge imbalances can be equalized. This can just be a passive connection to ground (figure \ref{fig:drl} a), but using active components on this path allows us to modulate the potential difference between the patient and system ground, which opens up the opportunity to both bias all signals to a known voltage, as well as cancelling predictable or low-frequency noise, such as mains hum, by feeding its opposite as a bias.

For the practicality of such a biasing circuit, it must connect to the patient in a relatively far away part of the body from what is being measured, so it will minimally interfere with the other electrodes,  with as little introduced differential voltages as possible. As per ECG doctrine, the right leg is often used, leading to the name "Driven Right Leg circuit", but this referencing electrode can be connected anywhere depending on the measurement at hand.

\subsubsection{Electrode types}

The first step we control in the signal chain is the point of contact with the patient. The electrode type and quality have a great influence on the overall design, performance, and subjective user experience.

In the simplest case, just a metallic electrode connected to the body can sense electrical field changes, forming a polarizable electrode, which cannot offer consistent current flow, but rather acts like a capacitor, wherein ion concentrations on the skin surface will repel or attract electrons in the electrode, creating small currents.

A much more advantageous setup is one in which the physiological ion currents can be directly converted to electrical current akin to a resistive interface, termed a non-polarizable electrode. By far the most commonly used electrodes are Ag/AgCl electrodes which fit into this category.\cite{Lee2008} These have a silver chloride surface layer, backed by a silver core, and historically other metals for cost reduction. These are commonly distributed as both dry electrodes, which contact the skin directly, but require an electrolyte solution to be applied for best results, as well as wet electrodes, with the electrolyte solution suspended in an elastic gel that adheres to both the electrode and skin, both illustrated in figure \ref{fig:agcl_photos}. The electrolyte solution provides free $\text{Cl}^-$ ions that facilitate the conversion of electric current to ionic current by ionic decomposition of the $\text{AgCl}$ layer or recombining $\text{Cl}^-$ ions with $\text{Ag}^+$ ions. Dry electrodes can also somewhat function without any electrolyte solution applied thanks to the dissolved $\text{NaCl}$ content of sweat, which also provides free chlorine cations.

Chemically, the $\text{AgCl}$ layer partially decomposes into $\text{Ag}^+$ and $\text{Cl}^-$ ions when exposed to the aqueous electrolyte solution. When the body potential is higher than the electrode potential, i.e. there's a lack of anions in the body, chlorine cations flow from the electrolyte gel towards the body, further encouraging the decomposition reaction $\text{AgCl} \rightarrow \text{Ag}^+ + \text{Cl}^-$ to replenish them. The positive $\text{Ag}^+$ can precipitate out on the electrode surface, exchanging an electron and thus inducing conventional electrical current "into" the electrode. On the other hand, when the body is more negatively charged than the electrode, $\text{Cl}^-$ ions in solution will be in excess and will recombine with $\text{Ag}^+$ ions on the $\text{AgCl}$ surface. The new lack of silver anions promotes the ionization of the metallic silver layer, releasing electrons. In both cases, the Ag/AgCl electrode and electrolyte solution act like a mostly resistive interface, with some stray parallel capacitance.

Because of their prevalence, Ag/AgCl electrodes are inexpensive and easily obtainable from general medical suppliers. Additionally, \cite{Lee2022} presents 3 methods for fabricating dry Ag/AgCl electrodes, which can be implemented even in small scales and using cheap materials, were these not commercially available.

The state of the art are conductive polymer PEDOT:PSS electrodes, thanks to their great mechanical, electrical, and chemical properties: They adhere and conform to skin, are flexible, self-healing, and have good biocompatibility, only falling behind the classic Ag/AgCl electrodes in terms of contact impedance, yet still being conductive enough for biopotential readings. \cite{Seiti2023,Hou2024}

\subsection{Market research}

Biopotential measurements are no new subject and many products already exist both in the academic and commercial fields. All are subject to engineering tradeoffs between parameters such as number of channels, sample rate, precision, noise, electrode types, etc., making each better suited to a certain use case. To the best of our research, there is no universal, modular and extensible solution to the problem of biopotential measurements. Further on, we overview some representative options for EMG / ECG DAQ systems:

\subsection*{OpenBCI}

The OpenBCI ("Open Brain-Computer Interface") project started in 2013 and provides open source boards mainly intended for EEG measurements, but which are also applicable for EMG and ECG. These have underwent a handful of revisions in the last decade, and the current generation consists of:

\begin{itemize}
    \item Cyton -- an 8 channel, 24 bit, DAQ based on the ADS1299. Retails for 917 Euro (as of 2024-08).
    \item Ganglion -- a 4 channel, 24 bit, DAQ based on the MCP3912. Retails for 458 Euro (as of 2024-08).
    \item Daisy -- an addon for the Cython providing 8 extra channels using a second ADS1299. Cython+Daisy package retails for 1835 Euro (as of 2024-08).
\end{itemize}

While the retail price on the OpenBCI store is steep, design files are provided for all products and they can be ordered and indivually built for a fraction of the cost. If built based on the schematics, the bulk of the cost will be the ICs, which add up to around 100 Euro (estimate as of 2024-08).

\subsection*{ASPEN}

\cite{Esposito2018}

What we're doing, but better, but a PhD thesis.

\todo{more :)}


\subsection*{Quanser QNET Myoelectric board}

The Quanser QNET Myoelectric trainer board is a closed-source, one channel addon board for the National Instruments Elvis II. It provides a fixed amplification stage of  powered by a battery to remove power supply noise, and further uses the ELVIS' oscilloscope channel for analog to digital conversion. Its signal chain consists of an overall gain of $300$, and first order LPF and HPF limiting the bandwidth to $20-500\Hz$.

Anecdotally, this is the board that sparked the idea for this project, as it is one of the experimental boards given to TUCN students.  It depends on the NI Elvis, only has one channel measurements, and is not open source. This makes it a good educational resource, but it falls short on both performance and cost.

\subsection*{BioWare EXG Pills}

As one of the cheapest related products, the EXG Pill is an open source instrumentation amplifier board meant to aid in biopotential data acquisition by connecting it to another device with an ADC, typically an Arduino board. It is available for order on the creator's online shop, but being an open hardware project allows anyone to manufacture their own, to specification. It is not an entire DAQ system, but rather just the amplification stage.

\subsection*{Cometa Pico EMG, Delsys Tringo}

As one of the most polished products on the market, the Cometa Pico EMG and Delsys Tringo are two competing multi-channel wireless EMG DAQ systems.

The Cometa Pico EMG supports up to 36 channels. Its sensors have a $2\kHz$ sample rate and an integrated IMU.\cite{cometapicoemg}

The Delsys Tringo has a more flexible system with a range of different sensor types: single bipolar sensors, grid array sensors, snap electrode sensors, which are more intended for high end research. Comparable to the Pico EMG, the Trigno Avanti sensor has a max sample rate of $4370\ \text{sps}$ and also have integrated IMUs.\cite{delsysemg}

On the other hand, being high-end commercial medical products, both systems cost orders of magnitude more than what we aim for (\cite{delToro2019} estimates 15000 Euro for the Cometa and 20000 Euro for the Delsys) and neither abide by the open source philosophy.

\subsection*{MyoWare}

Advancer Technologies's line of MyoWare products started out in 2015 with their first generation MyoWare muscle sensor, which similarly to the BioWare EXG Pills described above, only provided the analog signal chain consisting of isolation, an adjustable amplifier and filtering, requiring an external ADC, with the Arduino Uno being recommended for small projects. The current generation has adjustable gain between $?-200$ and a bandpass filter between $20-500\Hz$.\cite{myoware}

\subsection{Noise sources}

The superposition of electrical signals measurable by biopotential DAQs contains a complicated mix of noise coming from various physiological and other external factors, besides the signal of interest. Additionally, the measurement circuitry compounds noise at every step. Modeling some of the main noise components of the entire signal chain is essential for its design and proper specification.

Some of the studied noise sources are \textit{intrinsic} to the process and impossible to remove, such as thermal noise and the amplifier and ADC noise profiles. Others are not intrinsic, depend on the setup, and can be compensated for, such as mains hum.

\subsubsection{Thermal noise}

Even with ideal conductors and measurements, any resistive circuit will be affected by thermal noise, as a consequence of Planck's law of blackbody radiation \cite[chapter 3.2]{Urick2015-pm}. An ideal resistor $R$ at absolute temperature $T$ will manifest thermal noise $V_n$ over a frequency window $\Delta f$ having mean squared value \( \overline{V_n^2} = 4k_BTR\Delta f \), where $k_B = 1.380649\cdot10^{−23} \unit{J\per K}$ is the Boltzmann constant.

For a rough estimate of the order of magnitude of thermal noise in our biopotential measurements, we study the following hypothetical setup: Assume the electrodes attached to the patient are in thermal equilibrium at a typical skin temperature of $T \approx 308 \K$ \cite{Suarez2016}, we are limiting the frequency band to $\Delta f = 500 \Hz$ by filtering, and experiencing a typical resistance ${R \approx 3500 \ohm}$ (electrode + contact + internal resistance) \cite{Kurniawan2022}. This is only useful as a rough order estimation, because the resistance will vary depending on the electrode location, surface preparation, measured individual, electrode condition, etc. The estimated thermal noise will is:

\begin{thesisequation}{thnoisevalue}
\overline{V_n^2} = 4k_BTR\Delta f \approx 0.02977 \uV^2
\end{thesisequation}

Which can alternatively be interpreted as having an RMS noise of $\sqrt{0.02977 \uV^2} \approx 172.5 \nV$ or a noise density of $\sqrt{4k_BTR} = 7.716 \nV/\sqrt{\Hz}$. 

\subsubsection{Amplifier and ADC noise} \label{ampandadcnoise}

Both ADCs and amplifier circuits have specified noise figures which pose design tradeoffs. In ADCs, the noise is specified as additive noise at the input $V_{na}$, such that a measurement $V_{meas}$ of a "real" voltage $V$ will be $V_{meas} = V + V_{na}$. Amplifiers are characterized by both input noise $V_{ni}$ created by the input stage, as well as output noise $V_{no}$ created by internal circuirty and the output stage. The output of an amplifier with gain $G$ is $V_{amp} = (V + V_{ni}) \cdot G + V_{no}$. As an easier to use figure, the \textit{Input Referred Noise} is the equivalent disturbance at the input that would create both the effects of input and output noise componded. $V_n = V_{ni} + V_{no}/G$ such that we can simplify to $V_{amp} = (V + V_n) \cdot G$. An equivalent block diagram is presented in figure \ref{fig:ampadcchain}.

\begin{thesisfigure}{Block diagram of amplifier and ADC signal chain, with modelled noise}{ampadcchain}

\begin{tikzpicture}[node distance=1.5cm]
    \node [] (V) {$V$};
    \node [] (s1) [right of=V] {$\oplus$};
    \node [] (Vni) [above of=s1] {$V_{ni}$};
    \node [draw,isosceles triangle] (G) [right of=s1] {$G$};
    \node [] (s2) [right of=G,node distance=2cm] {$\oplus$};
    \node [] (Vno) [above of=s2] {$V_{no}$};
    \node [] (Vamp) [right of=s2] {$V_{amp}$};
    \node [] (s3) [right of=Vamp] {$\oplus$};
    \node [] (Vna) [above of=s3] {$V_{na}$};
    \node [draw] (ADC) [right of=s3] {ADC};
    \node [] (Vmeas) [right of=ADC,node distance=2cm] {$V_{meas}$};

    \path[draw,->]
        (V) edge (s1)
        (s1) edge (G)
        (G) edge (s2)
        (s2) edge[-] (Vamp)
        (Vamp) edge (s3)
        (s3) edge (ADC)
        (ADC) edge (Vmeas)
        (Vni) edge (s1)
        (Vno) edge (s2)
        (Vna) edge (s3);
\end{tikzpicture}

\rotatebox[origin=c]{90}{\Large$\approx$}

\begin{tikzpicture}[node distance=1.5cm]
    \node [] (V) {$V$};
    \node [] (s1) [right of=V] {$\oplus$};
    \node [] (Vn) [above of=s1] {$V_n$ ("IRN")};
    \node [draw,isosceles triangle] (G) [right of=s1] {$G$};
    \node [draw] (ADC) [right of=G,node distance=2cm] {ADC};
    \node [] (Vmeas) [right of=ADC,node distance=2cm] {$V_{meas}$};

    \path[draw,->]
        (V) edge (s1)
        (s1) edge (G)
        (G) edge (ADC)
        (ADC) edge (Vmeas)
        (Vn) edge (s1);
\end{tikzpicture}
\end{thesisfigure}

This raises one of the most important design decisions of the signal chain, namely deciding on the amplification structure before the ADC. Using no amplifier at all is the most economic and also avoids $V_{ni}$ and $V_{no}$, but in the case of very small input signals, they are left to be dominated by ADC noise $V_{na}$. Adding an amplifier with unity gain only compounds amplifier noise and is as such detrimental, but further increasing the gain leads to better signal-to-noise ratio (SNR):

\begin{thesisequation}{snr}
\text{SNR} = \frac{\text{"signal"}}{\text{"noise"}} = \frac{G\cdot V}{G\cdot V_{ni} + V_{no} + V_{na}} = \frac{V}{V_{ni} + (V_{no} + V_{na}) / G} \longrightarrow \frac{V}{V_{ni}}
\end{thesisequation}

Thus, given an amplifier with known $V_{ni}$ we can compute the best-case noise figure assuming "infinite" gain. The rationally convergent nature of the above expression inevitably creates a situation of diminishing returns, wherein increasing the gain will always increase the signal quality, but even relatively small gains achieve practically ideal performance.

The drawback of having very large gain is the limit it incurs on the measurable range of input voltages. Any analog circuit can only handle a limited interval of voltages, which when exceeded leads to unreliable measurements, with the possibility of permanent damage to the equipment. If the ADC is limited to measuring $\pm V_{max}$, the measurable signals are implicitly limited to $\pm V_{max}/G$.


In this topology consisting of a single amplifier stage characterized by $V_{ni}$ and $V_{no}$, followed by an ADC with input noise $V_{na}$, measuring the "true" voltage $V$, the resulting digitized sample $V_{meas}$ can be decomposed into:

\begin{thesisequation}{decomposey}
V_{meas} = GV + GV_{ni} + V_{no} + V_{na}
\end{thesisequation}

Which can be rearranged to obtain an expression for RMS total noise $V_n$:

\begin{thesisequation}{rmsnoise}
\overline{V^2} = \frac{\overline{V_{meas}^2}}{G^2} + \underbrace{\left(\overline{V_{ni}^2} + \frac{\overline{V_{no}^2} + \overline{V_{na}^2}}{G^2}\right)}_{\overline{V_n^2}}
\end{thesisequation}

\subsubsection{Flicker noise}

Especially prevalent in BJT transistors and devices built using them, flicker noise is a low-frequency phenomenon where various material defects induce fluctuations with power density $P \propto 1/f$. It is also manifested in other active devices (MOSFETs, JFETs) and passive components, with a mix of various mechanisms contributing to it, many of which not fully agreed upon. A practical yet relatively complex solution to flicker noise is modulating the signal of interest to higher frequencies using a chopper and lock-in amplifier. \cite{flickernoise}

\subsubsection{ECG noise models}

Besides the previous intrinsic kinds of noise, further disturbances in ECG measurements can be classified, as per \cite{Chatterjee2020}, into: low frequency ($< 0.5\Hz$) baseline wander caused by body motion and electrode contact issues, power-line artifacts ($50/60\Hz$ and their harmonics) caused by insufficient power supply decoupling when the measurement device is connected to mains power and capacitive or inductive coupling between the patient and surrounding mains lines, and muscle artifacts (EMG signals). Of the three, the first two are relevant and undesireable in any biopotential measurement, while the latter is obviously the signal of interest in EMG applications.

From a signal processing standpoint, a simple solution for low frequency wander and power line artifacts is using a DC blocking comb filter, but these can also be addressed in the hardware design, especially when it comes to power supply decoupling.

\subsection{Signal chain design}

With today's available technology, signals in the millivolt range are relatively easy to measure even at quite high rates, if strongly driven. Intrusive methods of biopotential acquisition such as needle electrodes plugged directly into muscle fibers can work with no amplification stage, and just a direct connection to the ADC board, while still having good signal quality. On the other hand, when measuring with surface electrodes, both the electrode and biological tissue become a very high impedance element in the signal chain, which means that even relatively high biopotentials will have a hard time overcomigng parasitic effects and signal chain noise before reaching the ADC.

\subsubsection{Instrumentation amplifiers}

Instrumentation amplifiers are a class of differential amplifier which boast high open-loop gain, low noise, and high input impedance. The typical textbook realization is with 3 operational amplifiers connected like in figure \ref{fig:inamp}, which allow the gain to be adjusted by choosing the value of resistor $R_G$. Their high input impedance means that very little current is needed to affect the inputs, helping with the issue of high electrode and tissue impedance. Their high power supply rejection ratio (PSRR) and common mode rejection ratio (CMRR) make them even more suitable for differential electrode applications, requiring very little extra circuitry.

\begin{thesisfigure}{Typical implementation of an instrumentation amplifier}{inamp}

\end{thesisfigure}

\subsubsection{Multiplexed ADCs}

\todo{write about them :)   - multiple channels, single converter, multiplexor (possible configurations - limited or arbitrary?), digital filtering, sequencer, conversion clock dependence on communication clock makes noise and is bad}

\subsubsection{EMI prevention over long wires}

When dealing with long wires and weak signals, electromagnetic interference (EMI) is a significant source of noise which is produced by virtually all electronic devices around. To combat this, common techniques are using twisted pairs and wire shielding. For simplicity, we will try to shorten the wires carrying weak signals as much as possible and only use strongly driven and high amplitude signals over long wires. The result is that in bipolar measurements, we place the differential amplifier as close as possible to the electrodes, and then don't care about signal degradation between the amplifier and ADC thanks to the strongly driven nature of that signal.

\subsubsection{Common collector voltage buffer}

Later used in both the preamplifier and Driven Right Leg circuits, the common collector amplifier, illustrated in figure \ref{fig:volbuf} is a minimal and cheap circuit for providing a low impedance, high drive strength "mirror" of an incoming signal with potentially higher impedance.

\begin{thesisfigure}{Common-collector voltage buffer}{volbuf}

circuit frumos :)

\end{thesisfigure}

Assuming the input voltage puts the transistor in the linear operating range, the output voltage will lie around:

\begin{thesisequation}{buffereq}
V_{out} = V_{in} - V_{BE} \approx V_{in} - 0.7\V
\end{thesisequation}

\newpage
\section{Analysis, design and implementation (30pg)}

\subsection{Objectives}

Additionally, we 

\todo{Justify objectives, format these boring bullet lists in a nicer way}

MUST:
\begin{itemize}
\item Open Hardware and So20ftware
\item 8 channels
\item Per-channel configuration
\item 1000Hz data rate (500Hz bandwidth) \todo{NNNNNOOOOOOOOO}
\item High CMRR, PSRR
\item High SNR (>40dB EMG; >20dB for ECG)
\item Open and extensible interfacing: ROS2, IIO
\end{itemize}

SHOULD (unchecked go to future work):
\begin{itemize}
\item 16 channels
\item Differential / single-ended configurability
\item Right Leg Drive
\item 1024Hz/2048Hz /channel data rate (1 Hz per FFT bin)
\item <1uV noise after filtering
\item 1uV precision
\end{itemize}

Guiding principles:
\begin{itemize}
\item Cost
\item Usability as an educational tool
\item Use Analog Devices parts
\item Modularity (decoupled hardware-firmware-software)
\end{itemize}

\todo{After finishing the prototype, make a comparison table between the already existing products and mine, especially on these objectives}

\newpage
\subsection{Electical part}

\subsubsection{Interfacing with the patient}

Taking from the research from section \ref{electrodetypes}, we do not consider PEDOT:PSS electrodes to be viable for this project due to them not being commercially available in standard and mass produced forms, being mostly a custom product experimented with in today's laboratories, even half a century after their discovery in the 1980s.

We settle for for snap-on adhesive Ag/AgCl gel electrodes thanks to being cheap, readily avaialble, standardized, and they can be connected to with inexpensive aligator clips, besides special (but more expensive) snap clips. The downsides are their low shelf life \todo{compare expired electrode signal quality with fresh electrodes once I get some fresh ones}, comparatively larger contact area (cca $200\mm^2$ vs \cite{Barbero2012}'s 2mm electrode diameters), and are lacking in subjective user experience, sometimes being very painful to detach.

\subsubsection{ADC}

We initially chose the AD7124-8\cite{AD7124-8} thanks to its very diverse feature set, including: programmable gain amplifiers (PGAs) which had the potential to remove the need for the external amplification stage, high precision, high number of channels, and high sample rate. Unfortunately, it is not a single cycle ADC. This means that although it has very good throughput on a single channel, when switching between two channels there is a dead time for the integrated filter to settle, which effectively reduces the per-channel sample rate to less than $100\Hz$ when sampling all 16, which is virtually useless for ECG ($f_\text{max}=150\Hz$) and EMG ($f_\text{max}=500\Hz$).

Our next choice was the AD4114\cite{AD4114}. It doesn't include PGAs, but otherwise has very good specificationss and has a much higher sample rate and a single cycle conversion mode.

The AD717X family of ADCs, which the AD4114 is part of (in spite of the numbering scheme), all expose a similar interface to the application developer. A number of channels can be enabled, which are then measured in a loop, in ascending order of their indices. Each of the 16 channels can select one of 34 inputs: 16 unipolar measurements ($V_{in,k} - V_{incom}$, 16 bipolar measurements ($V_{in,2k} - V_{in,2k+1}$ and $V_{in,2k+1} - V_{in,2k}$), an internal temperature sensor or the voltage reference itself. If an input is selected by multiple channels, it is read multiple times in the sequence. To parametrize a channel's conversion, one of 8 "setups" can be assigned. Setups describe whether a measurement is unipolar of bipolar, the used reference, buffering and filtering settings. Each setup has a corresponding digital filter with an internal state. If multiple channels refer to the same setup, they will also be alternately using the same digital filter, requiring some extra logic for avoiding latent (i.e. "hidden") samples from passing from one channel's to another's readouts. Conveniently, the \texttt{SING\_CYC} flag puts the ADC in "single cycle mode", which always flushes out the digital filter when switching channels, at the expense of worse filter performance, data rates, and greater noise.

The AD4114 lacks an internal sample buffer, meaning that data from a conversion must be read before the next one or else it is lost. While the digital interface allows for an interrupt signal on the \drdy line to notify the host of new data, it is incompatible with most microcontrollers' hardware SPI implementations and it is much more suitable for an FPGA host. Therefore we can and should resort to polling the status register to check for new samples, which are announced through the \texttt{RDY} bit. Polling should be done at least as fast as the conversion rate, but SPI transactions are cheap enough to poll even an order of magnitude more often than samples are coming in.

One of the most important limitations in determining which parameters to run the ADC in is its noise, which varies with the sample rate. While high sample rates are very desirable given we have practically limitless digital signal processing capacity, they also degrade the signal quality. This is especially relevant when sampling multiple channels, where the Output Data Rate (ODR) is divided between them. A single channel measurement with $\ge 500\Hz$ bandwidth can be done with the $1007\Hz$ setting for an effective resolution of $17.3\text{ bits}$, but having 6 channels forces the use of the $6211\Hz$ setting with $15.4\text{ bits}$ of resolution, and more channels than that start limiting the bandwidth to less than $500\Hz$.

For this purpose, we selected 7 configurations depending on the number of required channels so as to maintain at least $1000\Hz$ per-channel sample rate (table \ref{tab:configs}). Measurements of up to $6$ channels (single-ended or differential alike) can use the better performing sinc5+sinc1 filter. Measurements of $7 - 10$ channels cannot achieve the desired sample rate without switching to the faster yet more noisy sinc3 filter. Measurements of $11 - 16$ channels cannot achieve the desired sample rate in any configuration using the AD4114, with the max per-channel sample rate degrading to $10309/16=644.3125\Hz$ for the maximum of 16 channels.

\begin{thesistable}{0.9}{l|llll}{Selected measurement configurations and their specifications}{configs}

N & ODR ($\Hz$) & Filter & Per-channel rate ($\Hz$) & Noise ($\uV$ RMS) \\\hline
$1$ & $1007$ & sinc5+sinc1 & $1007$ & $20$ \\
$2$ & $2597$ & sinc5+sinc1 & $1298.5$ & $38$ \\
$3$ & $3115$ & sinc5+sinc1 & $1038.33$ & $41$ \\
$4$ & $4444$ & sinc5+sinc1 & $1111$ & $56$ \\
$5$ & $5181.65$ & sinc5+sinc1 & $1036.33$ & $64$ \\
$6$ & $6211$ & sinc5+sinc1 & $1035.17$ & $74$ \\
$7+$ & $10309$ & sinc3 & $10309/N$ & $1068$

\end{thesistable}

\subsubsection{Amplification stage}

The chosen AD4114 ADC has $N=24$ bits of resolution with one LSB equating to the following in the bipolar measurement mode.
\begin{thesisequation}{lsb}
\text{LSB} = V_{REF} / (2^{N-1} \cdot 0.1)
\end{thesisequation}

Using the internal $2.5\V$ reference, $\text{LSB} \approx 2.98\uV$. The noise level varies depending on the sample rate. At the single-channel $f_s=1007\Hz$, 6 channel $f_s=6211\Hz$, and all channel $f_s=10309\Hz$, the ADC noise is specified to be around $\overline{V_{na,1007}^2}=20\uV$, $\overline{V_{na,6211}^2}=74\uV$ and $\overline{V_{na,10309}^2}=1068\uV$ RMS respectively. Without an amplification stage, this would result in the following SNRs relative to a $1\mV$ signal:

\begin{thesisequation}{SNR_no_amp}
\begin{aligned}
\text{SNR}_{(\dB)} &= 10\log_{10}\left(\frac{(1\mV)^2}{\overline{V_{na}^2}}\right) \\
\text{SNR}_{1007} &\approx 34\dB \\
\text{SNR}_{6211} &\approx 23\dB \\
\text{SNR}_{10309} &\approx -0.6\dB
\end{aligned}
\end{thesisequation}

In the case of 7+ channels, a $1\mV$ signal slightly dips under the noise floor, becoming unmeasurable. Thus, a preamplification stage is required for enabling the use of all channels.

We choose the AD8226 \cite{AD8226} instrumentation amplifier as the central component of the preamplification stage. It is inexpensive, readily available, has more than sufficient CMRR ($>80\dB$) and can be configured for a gain of $G=\numrange{1}{1000}$ using a resistor $R_G = 49.4\kohm / (G-1)$. Its RMS voltage noise for our desired bandwidth of $\Delta f=500\Hz$ is:

\begin{thesisequation}{8266_noise}
\begin{aligned}
\sqrt{\overline{V_{ni}^2}} = \sqrt{500\Hz \cdot (22\nV/\sqrt{\Hz})^2} = 0.492\uV \\
\sqrt{\overline{V_{no}^2}} = \sqrt{500\Hz \cdot (120\nV/\sqrt{\Hz})^2} = 2.683\uV
\end{aligned}
\end{thesisequation}

With the preamp stage added before the ADC, the total noise of a sample and the theoretical SNR are:

\begin{thesisequation}{noise_amp}
\begin{aligned}
\text{SNR}_{(\dB)} &= 10\log_{10}\left(\frac{(1\mV)^2}{\overline{V_{n}^2}}\right) \\
(\ref{eq:rmsnoise}) \Rightarrow \text{SNR}_{(\dB)} &= 10\log_{10}\left(\frac{(1\mV)^2\cdot G^2}{\overline{V_{ni}^2}\cdot G^2 + \overline{V_{no}^2} + \overline{V_{na}^2}}\right)
\end{aligned}
\end{thesisequation}

As described in section \ref{ampandadcnoise}, increasing the gain will increase SNR but with diminishing returns, and it will limit the available measurement range. With the specified values for the AD8226, we compute a handful of gain scenarios (table \ref{tab:gains}) and choose $G\approx 20$ as a fair middleground between good SNR and good input range, allowing for some fluctuations without saturating the sensors. We use the part's gain formula to compute a suitable gain resistor:

\begin{thesisequation}{}
R_G = \frac{49.4\kohm}{G-1} = \frac{49.4\kohm}{20-1} \approx 2600\ohm
\end{thesisequation}

The closest E6-series value is $R_G = 2200\ohm$ which would give a gain of $G\approx 22.45 \approx +27\dB$.

\begin{thesistable}{0.4}{l|ll}{Full scale, SNR for various gains}{gains}
Gain & Approx. range & $\text{SNR}$ \\ \hline
1    & $5\V$      & $34\dB$ \\
2    & $2.5\V$    &  $40\dB$ \\
5    & $1\V$      &  $48\dB$ \\
10   & $500\mV$   &  $54\dB$ \\
20   & $250\mV$   &  $59\dB$ \\
50   & $100\mV$   &  $64\dB$ \\
100  & $50\mV$    &  $65\dB$ \\
1000 & $5\mV$     &  $66\dB$ \\
$\infty$ & $0$    & $66.16\dB$
\end{thesistable}

Using an active element (the instrumentation amplifier) also gives us the opportunity to do some hardware filtering. To combat high frequency noise, we add a first order RC low-pass filter with a cutoff of $f_c \approx 500\Hz$. Our implementation uses $R=330\ohm$ and $C=1\uF$ for a theoretical $3\dB$ cutoff frequency of:

$$f_c = \frac{1}{2\pi \cdot 330\ohm \cdot 1\uF} = 482.28\Hz$$

To encode the bipolar differential measurement as a unipolar signal, we bias the amplifier's output voltage using the AD8226's $V_{REF}$ input. As specified in the datasheet, this pin must be driven with less than $2\ohm$ impedance, so just a simple resistor divider wouldn't suffice. We use a common-collector voltage buffer after a $10\kohm - 10\kohm$ divider to amplify the drive strength, biasing the output to approximately $V_{REF} \approx 5\V / 2 - 0.7\V = 1.8V$.

To help with transient power supply fluctuations, the instrumentation amplifier's positive supply as well as $V_{REF}$ are decoupled with $0.1\uF$ capacitors. The second should also reduce BJT flicker noise.

The overall circuit diagram and a picture of the built amplifier board are presented in figure \ref{fig:ampboard}. Experimental validation of the preamplifier board is covered later in the paper.

\begin{thesisfigure}{Circuit diagram and photo of built amplifier board}{ampboard}

\includegraphics[width=0.48\linewidth]{ampboard_schematic.png} 
\includegraphics[width=0.48\linewidth]{ampboard_photo.jpg}

\end{thesisfigure}

\subsubsection{Microcontroller}

For microcontroller selection, the main performance considerations are on the one hand the ability to communicate via SPI fast enough to comply with the desired sample rate, and on the other hand for it to have a fast enough clock rate and memory for the IIO Daemon which will comprise most of the final application. For further work, networking capabilities would be a nice-to-have to be able to run a standalone ROS2 node. A plethora of well known boards match these criteria, among which the very popular Espressif ESP8266 and ESP32, or the Raspberry Pi RP2040 and RP2350 would be sufficient and good alternative choices thanks to their excellent community support. The popular, yet more and more obsolete ATMEGA328p of Arduino fame isn't fast enough for both recording and further transferring all 16 channels, especially not through unmodifier IIOD / ros-micro implementations. We chose the Analog Devices MAX78000FTHR board thanks to its powerful specs which would also allow for some degree of digital signal processing on the board itself. We will use the MAX78000's dedicated SPI bus which is exposed on the feather board.

\todo{MAX78000 pinout diagram?}

\newpage
\subsection{Firmware part}

\todo{Short 100-200 word intro about firmware}

\todo{The no-OS framework}

No-OS is a firmware framework developed by Analog Devices that defines a common API for accessing common peripherals on embedded systems. While its scope somewhat overlaps with a typical Hardware Abstraction Layer (HAL) within an Operating System, no-os focuses on providing minimal abstractions to run directly on bare metal, as the name suggests. Additionally, it provides abstractions for the platform architecture, build systems and custom vendor SDKs, such that code can be very easily ported from one platform to another.

The benefits of using such a versatile framework were constantly apparent during this project's development, which required us to change the microcontroller platform, peripherals, as well as communication protocols during protoyping. While each change required small rewrites of the codebase, the uniform structure of no-os meant we didn't have to rewrite thousands of lines of code after each hardware change.

\subsubsection{Serial communication with host}

In order to send data to the attached computer or carrier board, the de facto solution is using UART-over-USB, which exposes an USB interface to the attached host and UART to the microcontroller. Most commonly, microcontrollers have dedicated circuitry for UART transmit and receive, which allows the rest of the program to run concurrently to serial communication. This is implemented using two FIFO buffers, one for data pending to be sent from the microcontroller (TX FIFO) and one for received data ready to be processed by the program (RX FIFO).

De facto, most microcontroller development boards, including the MAX78000FTHR, MAX-Arduino, and the more common ATMEGA368P "Arduino-like" boards provide UART-USB translation via a dedicated IC, abstracting away the complexities of the USB communication stack.

UART modules have varying degrees of configurability of the baud rate, flow control, and framing. For most practical uses, though, including this project, one may just specify the baud rate, defaulting to 8N1 (8 data bits, no parity bit, 1 start bit, 1 stop bit) framing and no flow control. Typical baud rates include 9600 baud, a legacy of 20th century POTS modems (introduced by the V.32 ITU-T standard \cite{}) and 115200 baud, common both in last-century modem systems, but also recently popularized by its default use in the Arduino development environment.

To achieve the required bandwidth for this project, a suitable baud rate must be chosen. Given the sample rate $f_s$, number of channels $N$, and number of bytes per channel sample $d$, and 8N1 framing (giving $10\text{ baud}/\text{byte}$), the baud rate $f_{baud}$ must be:

\begin{thesisequation}{baud}
f_{baud} \geq f_s \cdot N \cdot d \cdot 10 = 1024 \cdot 16 \cdot 3 \cdot 10 = 491520\text{ baud}
\end{thesisequation}

Additionally, choosing a baud rate larger than this minimum will allow for less timing concerns, especially when it comes to not overflowing the receive and transmit buffers.

Baud rate selection is not universal across devices and the firmware for each hardware platform must choose a rate that is both compatible with the hardware and that satisfies relation \ref{eq:baud}. In the case of the MAX78000FTHR, a rate of exactly $491520\text{ baud}$ can be achievend by using the IBRO (Internal Baud Rate Oscillator) of $7.3728\text{MHz}$ with a clock divider of $15$, but [we] will use a divider of $9$ for a "round" value of $819200\text{ baud}$ to have a good margin of error and not need such strict synchronization between the UART module and the rest of the program.

\subsubsection{SPI Communication basic proof of concept (2024-05 single channel demo)}

For initial validation of the concept on the AD4114, \todo{write about the PoC: 1 channel, no amplification, full sample rate, can see both EMG and ECG on it. can do nice graphs, can diagnose my RBBB}

\subsubsection{No-OS Driver}

Fortunately, the no-OS ecosystem already provides drivers for both the MAX78000 platform as well as the AD4114 as part of the AD717X family of ADCs\cite{ad717x}. What's left is to 

\subsubsection{IIOd integration}

Defining the structure of the IIO context is a very important step as it solidifies a mapping between the device-specific functionality on the hardware side and the application-specific functionality on the software side. Choosing a versatile collection of attributes to configure the device and channels to structure the data is no trivial task with no unique solution. The exposed attributes may encode very low level details, as in {find an AD part with a very bare-metal attribute mapping}, or more high level ones. Choosing which configuration is to be specified on the software side and which configuration is to be derived from that on the firmware side {of pula mea ce scriu aici... vreau sa zic ca nu o sa expun direct registri, ci o sa expun chestii mai high level: frecventa de mains hum (50/60), parametri de filtrare, poate sample rate daca o sa imi dau variante (posibil tho sa fie fix la 1k), si pe baza lor se ocupa firmware sa puna registrii cum trebuie.

For lower-level functionality, an IIO debug interface is also specified, which allows for direct manipulation of the ADC's registers. This shouldn't be normally used, but allows for total control of the device in case the user needs it.

1. Defining the desired iio context structure: what do we expose, in which formats, etc
2. Handler functions
3. Boom done, connect to it from software

\subsubsection{Continuous and asynchronous data acquisition using IIO triggers}

By default, the expected behaviour of IIO devices is to start acquisition on selected channels when requested (via an \texttt{OPEN} command), and stop and disable the channels once the requested number of samples is reached. In low sample rate scenarios, the host may request the next acquisition window very soon after the current one finishes, and assuming the time delay between windows can be minimized with respect to the overall window size, this could prevent missing any samples and only introduce a small amount of phase drift. Additionally, there are high-speed applications in which continuity between acquisition windows is not strictly required. On the other hand, with \our roughly $16\kHz$ sample rate (cca. $60 - 62\us/\text{sample}$) and requirement to not miss any samples, this application has very little room for error in timing. As per \cite{linuxlatency}, modern real-time linux setups have scheduler latencies in the tens and hundreds of microseconds, which realistically leaves no time to actually receive the current window's buffer and send a new acquisition request without missing any samples, even if the acquisition channels were left enabled.

\todo{figure, vertical stack of sequence diagrams of: normal iio polling with phase drift in slow ADC, fast ADC - loss of samples, trigger and buffer}

With this general issue in mind, IIO defines triggers as software or hardware events that initiate data acquisition without requiring regular polling from the host. Data captured as a result of triggers is stored in circular buffers and provided with minimal latency whenever the host polls next.

With the AD4114 in continuous conversion mode, two trigger sources are apparent: using the \drdy\ behaviour in continuous conversion mode (as described in \ref{label:drdy}) or using a timer to poll the AD4114 at least as fast as the conversion rate. While the first option is more elegant and wouldn't do any unnecessary polling, it requires nonstandard operation of the SPI protocol by keeping CS active and monitoring the MISO (\drdy) line, which isn't generally possible unless \we bitbang the SPI protocol or have access to more configurable IO options like on FPGA platforms or the Programmable Input and Output subsystem of RP2040 fame (\cite{Smith2021}). The second option of setting up a timer interrupt which polls the AD4114 status register to check if a new conversion result is available

\todo{more :)}

\subsubsection{ROS2 on network-capable microcontrollers}

In recent years, there has been a wave of developments in fast, network-capable, cheap microcontroller platforms, leading to products such as the Espressif ESP8266 and ESP32, or the Raspberry Pi Pico-W, which are capable of running ROS2 nodes all by themselves. 

\todo{more :)}

\subsubsection{Digital filtering of the signal}

Although the digital signal processing part of such biopotential measurement systems is very important for a full product, it is not the main topic of this thesis, and yet the chosen system architecture offloads most of the signal conditioning to the digital domain. The main source of noise is mains hum, with a fundamental frequency of $50\Hz$ and various harmonics, which can all be removed using a notching filter, resulting in minimal loss of information in other portions of the frequency spectrum. The Qaulity factor $Q$ of the filter controls how narrow the attenuated frequency bands are, and it can be adjusted. It poses a tradeoff between good noise rejection and signal quality, and is designed to be adjustable. Experimentally, we found values of around 5-10 to give the best waveforms for visual inspection. Figure X illustrates the tradeoff between small Q doing ??? and high Q doing ???.

Taking from scipy's implementation of an IIR Notching filter (`scipy.signal.iircomb`), I also use the structure described in {cite: Sophocles J Orfanidis, "Introduction to Signal Processing", Prentice-Hall, 1996, ch. 11, "Digital Filter Design"} thanks to its adjustable Q factor, numerical stability even at higher orders, and default rejection of the DC component of the signal, which are all very useful for the required signal conditioning. Parametrized by the the sampling frequency $f_s$, the cutoff freqiency $f_0$ (which must be an integer divisor of $f_s$), quality factor $Q$, the IIR filter $H(z)$ is computed as:

\begin{gather*}
N = f_s / \omega_0 \overset{!}{\in} \mathbb{N} \\
\omega_0' = \frac{2\pi\omega_0}{f_s}  \\
\omega_\Delta = \frac{\omega_0'}{Q}  \\
\beta = \tan(N * \omega_\Delta / 4)  \\
a = \frac{1 - \beta}{1 + \beta}
\end{gather*}
\begin{thesisequation}{filter}
H(z) = \frac{1}{1 + \beta} \cdot \frac{1 - z^{-N}}{1-az^{-N}}
\end{thesisequation}

\todo{check I transcribed these correctly and actually implement them myself! I only blindly copied these for now}

\newpage
\subsection{Software part}

\subsubsection{ROS2 agent for non network-capable microcontrollers}

In the case of microcontroller platforms that aren't network capable, including the MAX78000FTHR, the device itself won't have a means of directly connecting to the ROS2 network as a node. As such, it needs to pass along the data to another device that can. Two implementation variants are immediately evident: Using the micro-ROS framework, letting ros logic take over the serial device instead of having it communicate using the IIO protocol, and running a ROS-micro agent on the host
- Using the ros-control framework and an IIO-ROS2 adapter on the host / carrier board.

Of which I chose the second because it doesn't require reconfiguring the serial port and allows for simultaneous use of ROS and IIO.

\todo{implement :)}

\subsubsection{libiio, pyadi-iio}

ROS is for robotics, what about just doing some basic signal processing in python without setting it up as a ros node? Libiio has got you covered! Pyadi-iio adds a cherry on top, exposing the entire system as a very simple to initialize and query python object.

\todo{implement :)}

\newpage
\section{Results and validation}

\subsection*{Preamplifier module}

We evaluate the designed preamplifier module's performance using two tests based on the ADALM2000 platform, which powers the module, generates test signals, and measures the amplified output.

For the first test, a sinusoidal $A_{test} = 50\mV$ peak-peak test signal is applied. The measured waveform displays the expected DC bias of $\overbar{V_{amp}}=1.88\V$, and an amplitude of $A_{amp}=1.23\V$ peak-peak, giving an effective gain of $G = A_{amp} / A_{test} = 24.6$, just within the $10\%$ tolerance of the designed $G=22.45$, justifiable by the tolerances of the passive components we used. For these amplitudes, the harmonic distortion and amplifier noise are below the $-80\dB\text{FS}$ noise floor of the ADALM2000.

Secondly, we test the frequency response using the Network analyzer feature of the Scopy software\cite{scopy}. A $10\Hz - 10\kHz$ sine sweep is generated for the amplifier's input, and the resulting output is measured, determining both the magnitude and phase relation, which are plotted in figure \ref{fig:preampbode}. Both the measured magnitude and phase characteristics match the ideal frequency response well within the $10\%$ tolerances of the passives. For frequencies above $1\kHz$, the signal amplitude decreases to small multiples of the ADALM2000 resolution, leading to increasing amounts of quantization noise, which can be seen in both the magnitude plot as an increased reported magnitude, as well as in the phase plot as phase noise. This isn't concerning, as it is a limitation of the ADALM2000 used for the test setup, not the preamplifier board itself. More importantly, the desired amplification and corner frequency are validated.

\begin{thesisfigure}{Measured preamplifier frequency response, compared to ideal component $+27\dB$ gain, $500\Hz$ first order LPF amplifier}{preampbode}
\begin{tikzpicture}

\begin{semilogxaxis}[
        width=14cm, height=5cm,
        xmin=10, xmax=10000,
        ylabel={Magnitude [dB]},
        grid=both,
        legend pos=south west,
        extra x ticks={500},
        extra x tick label={500}
    ]
    \addplot[color=black, thick] table[x index=0, y index=3] {../data/preampbode.dat};
    \addlegendentry{Measured}

    \addplot[color=cyan, thick, dashed] table[x index=0, y index=1] {../data/preampbode.dat};
    \addlegendentry{Ideal}
\end{semilogxaxis}

\end{tikzpicture}
\begin{tikzpicture}

\begin{semilogxaxis}[
        width=14cm, height=5cm,
        xlabel={Frequency [Hz]}, xmin=10, xmax=10000,
        ylabel={Phase [deg]}, ymin=-90, ymax=0,
        grid=both,
        legend pos=south west,
        extra x ticks={500},
        extra x tick label={500},
        ytick={0, -20, -45, -70, -90}
    ]
    \addplot[color=black, thick] table[x index=0, y index=4] {../data/preampbode.dat};
    \addlegendentry{Measured}

    \addplot[color=cyan, thick, dashed] table[x index=0, y index=2] {../data/preampbode.dat};
    \addlegendentry{Ideal}

    % Add a specific point with a label
    \addplot[color=black, mark=*, mark size=3] coordinates {(500, -45)};
    \node at (axis cs:500,-45) [anchor=south west] {$-45^\circ\text{ at }500\Hz$};
\end{semilogxaxis}
\end{tikzpicture}
\end{thesisfigure}

\subsection*{Multi-channel EMG}

With EMG signals above the circa $1\mV$ noise threshold of the max data rate conversions, measuring 16 single-ended channels can be done without using 16 preamplifier boards, albeit at a loss of signal quality. For evaluation, we do a 6 channel measurement on the inner forearm, targeting multiple muscles used in wrist and finger flexion, and do an exercise with both low effort and high effort movements, to observe both individual action potentials, as well as complex MUAPs. (figure \ref{fig:emgresult})

\begin{thesisfigure}{6 Channel EMG measurement without preamplification, displaying an initial period of inactivity, a period of high effort movement (first box), and two individual action potentials during low effort movement (second and third boxes)}{emgresult}
\includesvg[width=0.8\linewidth]{emg6}
\end{thesisfigure}

In the frequency domain, the highest signal component is $-73\dBV$ at $110\Hz$, and the noise floor is around $-85\dBV$, for an SNR of $12\dB$ without preamplification.

\subsection*{Multi-channel ECG}

As proof-of-concept for ECG, we set up a measurement for ECG Leads I (Right Arm - Left Arm), II (Right Arm - Left Leg), III (Left Arm - Left Leg), referenced to the Right Leg through the Driven Right Leg circuit. A 5 second measurement was taken without preamplification and filtered using the MATLAB Signal Processing Toolbox \texttt{lowpass} function.

Because the ECG signal has a more restrained frequency composition, aggressive digital filtering can significantly help the signal quality. Before filtering, the noise floor lied around $-110\dBV$ and the lead II signal's peak was $-90\dBV$ at $13.5\Hz$ for an SNR of $20\dB$. After filtering, the noise floor dropped to $-160\dBV$ for an improved SNR of $70\dB$.

\begin{thesisfigure}{ECG leads I, II, III measurement without preamplification}{ecgdemo}
\includesvg[width=0.8\linewidth]{ecg3}
\end{thesisfigure}

\subsection*{Single-channel ECG}

To demonstrate both the preamplifier and best ADC SNR, we measure a single channel ECG, using the V1 lead. The positive electrode is placed vertically between the 4th and 5th rib, $4\cm$ to the left of the sternum, and the negative electrode on the back of the patient, approximately symmetrical to the positive electrode relative to the heart. Signal quality is much better than without the preamplifier, with a signal peak of $-48\dBV$ at $2\Hz$ and noise floor at $-120\dBV$, for a digitally unprocessed SNR of $72\dB$, which is on par with high-performance commercial systems.

\begin{thesisfigure}{Single channel ECG measurement of lead V1 with preamplifier stage}{rbbb}
\includesvg[width=0.8\linewidth]{ecg1_rbbb}
\end{thesisfigure}

Of morbid curiosity, this ECG (figure \ref{fig:rbbb}) displays two R waves, the normal first peak, and a delayed and smoother R' wave $\sim 170\ms$ later, confirming the previously clinically diagnosed RBBB of the author. A healthy individual without RBBB would only show the first R wave as a peak, followed by the S wave as a valley, and no other features until the following heartbeat.

\subsection*{EEG}

While outside of the scope of this project, we attempted EEG measurements, inspired by OpenBCI's architecture which supports all three interchangably. Naive electrode placement and the much weaker nature of EEG signals meant there were no discernable features above the noise floor.

\subsection*{LibIIO}

The data for the previous plots in this section was recorded using iio-oscilloscope\cite{osc} which is based on libiio, and later processed and plotted in MATLAB. All relevant features function: selecting channels, assigning inputs to channels, querying DAQ parameters (sample rate, per-channel scale and offset); thus valing the libiio component of the project.

\subsection*{ROS2}

The maximum throughput case is when measuring $7-16$ channels with a total data rate of $10309\Hz$ divided between them. We test the ROS2 publisher node and monitor its output using plotjuggler\cite{plotjug} to successfully verify it can record and publish samples at this rate. A screenshot of the measured EMG is shown in figure \ref{fig:plotjuggler}.

\begin{thesisfigure}{ROS2 Plot Juggler displaying measured and published 16 channel EMG at a per-channel data rate of $10309\Hz/16 = 644\Hz$}{plotjuggler}
%\includegraphics[width=0.8\linewidth]{plotjuggler.png}
\todo{here come the screenshot}
\end{thesisfigure}

\newpage
\section{Conclusion (1-2pg)}

We present a hardware + software solution intended for easy and affordable maintainability, using off-the-shelf components and open source hardware and software, capable of doing biopotential measurements of up to 16 single-ended or differential channels, with $>1000\Hz$ sample rates in all configurations. Differential measurements can be made directly or by use of a differential preamplifier module with a gain of $24$ and active low pass filtering ($500\Hz$). 

\subsection{Future work}

While initial ambitions were high, the project fell short of all proposed objectives. 

Among initial ambitions which unfortunately 

\section*{Acknowledgements}

I would like to sincerely thank Tassos Natsakis for his guidance and unending patience, Mark Thoren for the incredibly helpful mixed-signal design resources, and Monica Ignat for kindly giving me the work flexibility I needed to finish this project.

\newpage
\section{Bibliography}

\printbibliography[heading=none]

\end{document}
